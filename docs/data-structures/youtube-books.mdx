---
title: YouTube & Books
sidebar_position: 8
---

# YouTube, Books, and More

Recommended textbook reading and YouTube channels for data structure concepts.

## YouTube creators to Subscribe
- Gaurav Sen Gaurav Sen - https://www.youtube.com/c/GauravSensei
- Kevin Naughton Jr. - https://www.youtube.com/c/KevinNaughtonJr
- Nick White - https://www.youtube.com/c/NickWhite
- Vivekanand - Algorithm Every Day - https://www.youtube.com/user/vivekanandkhyade
- WilliamFiset - https://www.youtube.com/c/WilliamFiset-videos


Data structures and Algorithms

- Data Structures & Algorithms in Java, Second Edition - Robert Lafore
- Elements of Programming Interviews (EPI)
- Cracking The Coding Interview - Gayle Laakmann McDowell
- [Designing Data-Intensive Applications](https://www.googleadservices.com/pagead/aclk?sa=L&ai=DChcSEwimwe2QodL5AhVIFdQBHbj5CuYYABAIGgJvYQ&ae=2&ohost=www.google.com&cid=CAESbOD2voWuRxZMFeKGSxQ1ddiRswKkWxihtbz3ytVLv6SDS8cpoyw8qEE4S1Bnk_ZkFwlv8CEAFBqR8THNB7zF-4mOPnFSNkyQwqnkCcZfqN-u2tojH0JW7rfwGNoVGQZBQn_eeMf7t6Xi12n3Rw&sig=AOD64_2GoSTEXJlCAzKS0MnVpIlr5RKcpQ&ctype=5&q=&ved=2ahUKEwiY8-OQodL5AhWVmGoFHfF5C84Q9aACKAB6BAgCEEQ&adurl=)

System Design

- [Hello Interview] (https://www.hellointerview.com/)
- [educative.io](http://educative.io/) - [Grokking the System Design Interview](https://www.google.com/url?sa=t&rct=j&q=&esrc=s&source=web&cd=&cad=rja&uact=8&ved=2ahUKEwignbCypp35AhXdIDQIHWHEAsAQFnoECAcQAQ&url=https%3A%2F%2Fwww.educative.io%2Fcourses%2Fgrokking-the-system-design-interview&usg=AOvVaw12wj_kVu8790QbCBtrxs0L)
- [System Design Interview – An insider's guide](https://www.amazon.com/System-Design-Interview-insiders-Second/dp/B08CMF2CQF)
- https://www.designgurus.io/

---

## Kevin Naughton's Facebook Playlist

Detailed walkthrough solutions and playlists for Facebook/Meta interview questions.

# Kevin Naughton's Facebook Playlist

- Anagram "anagram", "nagaram" => true
    1. miantain an integer array of size 26
    2. parse String1 & increment the count at index count[s.charAt(i) - 'a']++
    3. parse String2 & decrement the count at index count[t.charAt(i) - 'a']--
    4. Loop count array and if you see any index non-zero, return false
- Valid Parantheses "(\{\})[]" => true
    1. Maintain a Stack
    2. When you see open parantheses, square bracket or curly brace; then push to Stack
    3. If you see any closed and if Stack is not empty & peek and check for its open; then pop() from Stack
    4. if none of the condition matches, return false
    5. At the end return if Stack is empty stack.isEmpty()
- Buy and Sell Stock [7,1,5,3,6,4] => 5
    1. Iterate the prices and if you have seen a price less than the min, then assign it to min
    for (int i =0; i &lt; prices.length; i++) if (prices[i] &lt; min) \{ min = prices[i]\}
    2. If not, calculate the max profit
    else \{ max = Math.max(max, prices[i]-min); \}
    3. return max
- Two Sum [2,7,11,15] target = 9 => [0,1]
    1. Maintain a HashMap
    2. Iterate and check whether the difference of target and the current element contains in the map
    3. If map contains the element, return the index of the current element and the index from the map
    4. If not, put the current element with its index
- Move Zeros [0,1,0,3,12] => [1,3,12,0,0]
    1. Have 2 pointers, write and read (other approach is just 1 index)
    2. If it is 0, just advance the read pointer
    3. If non zero, copy value from read pointer to write pointer
- First Bad Version
    1. since the version are sorted, do a Binary Search and look for first bad version
    2. if mid is not bad version; left = mid+1;
    3. else right = mid;
    4. return left
- Find the Celebrity
    1. Iterate the array and check for knows() and assign a Celebrity
    2. Iterate the array again except the celebrity from the above step
    3. if the person doesn't know the celebrity or if the Celebrity know that person then return -1;
    4. return the celebrity
- Meeting Rooms - whether the person can attend all meetings [[0,30], [5,10], [15, 20]] => false
    1. Brute Force - Compare interval with every other interval TC: O(n^2)
    2. Optimal solution: Sort the given interval by stat time
        1. Compare if the end time of first interval is > start time of second interval then return false; if (intervals[i][1] > intervals[i + 1][0]) return false;
        2. return true;
- Hamming Distance (XOR)
- Buy and Sell Stocks multiple transactions
    1. If [i+1] > [i]; calculate profit. profit += prices[i+1] - prices[i]
    2. return profit
- Single Number II [2,2,3,2] => 3
    1. Iterate the arrray and created HashMap with each element as Key and the number of occurances as value. map.put(A[i], map.getOrDefault(i, 0) + 1)
    2. Iterate the map and return the element with value/occurance 1. for (int key: map.keySet()) \{ if (map.get(key) == 1) return key;\}
- Letter Combinations "23" => ["ad", "ae", "af", bd,be,bf,cd,ce,cf]
    1. Create a mapping for all digits from 2 to 9 ["0", "1", "abc", "def","ghi","jkl","mno","pqrs","tuv","wxyz"]
    2. Call a recursive function passing the resultList, digits, currentChar, index and the above mapping
    a. In the recusive function, get the mapping character for the digit at index and
    b. recursively call the function for all characters; until index reaches the length of the digits
    
    ```java
    void letterCombinationsRecursive(List&lt;String> result, String digits, String current, int index, String[] mapping) {
    if (index == digits.length()) {
    result.add(current); //r["ad","ae", "af"]
    return;
    }
    String letters = mapping[digits.charAt(index) - '0'];// "abc" //"def"
    for (int i = 0; i &lt; letters.length(); i++) {
    letterCombinationsRecursive(result, digits, current + letters.charAt(i), index+1, mapping);
    }
    }
    ```
    
- Valid Palindrome
    1. Have 2 pointers - start & end
    2. Move both the pointers until it is a character
    3. If they both are not equal, return false
    4. return true
- Container with most water
- Monotonic array. Array either in increasing or decreasing order
    1. If the next element is less than the previous element, it is not in increasing order; set increasing flag to false
    2. If the next element is greater than the previous element, it is not in decreasing order; set decreasing flag to false
    3. return increasing or decreasing
- Sum of Left Leaves
    1. If root is null; return 0
    2. If the root has a left child & if it is the leaf node; then add its value with the recursive call for the right child
    3. If not recursively call for its left and right child
    public int sumOfLeftLeaves(TreeNode root) \{
    if (root == null) \{
    return 0;
    \} else if (root.left != null && root.left.left == null && root.left.right == null) \{
    return root.left.val + sumOfLeftLeaves(root.right);
    \} else \{
    return sumOfLeftLeaves(root.left) + sumOfLeftLeaves(root.right);
    \}
    \}
- Remove Duplicates from Sorted Array
    1. Create an index at index1
    2. Iterate the array, if curren elemnet is not equal to next element
    Write next element at index position
    3. return index
- Add Binary
- Reverse Linked List
    
    Approach# 1
    
    1. Push all Nodes to a stack
    2. Create a ListNode dummy and assign it to head
    3. Pop node and build a LinkedList with a dummy head
    4. Return dummy.next;
    Approach# 2
    5. Create a ListNode reversed and assign head to reversed
    6. Create a ListNode listToDo point to head's next node
    7. Initialize reversed.next to null
    8. Iterate all nodes while STack is not empty
    a. Assign listToDo node to temp
    b. Move listToDo to next
    c. temp.next = reversed
    d. reversed = temp
    9. Return reversed
- Task Scheduler
- Decode Ways
    
    String s = "226"; // BZ, BBF, VF => 3
    String s = "111"; // AAA, AK, KA => 3
    
    1. Dynamic Programming - Bottom up approach
    2. dp[] = new int[s.lenghth()+1] to consider the whole String
    3. Base case: 0=1; 1=if '0' 0 not 1
    4. Starting from 2; check for decode ways for first and second digit
    5. return the index of string length in dp array
    
    int numDecodings(String s) \{
    int[] dp = new int[s.length()+1];
    dp[0] = 1;
    dp[1] = s.charAt(0) != '0' ? 1 : 0;
    for (int i = 2; i &lt;= s.length(); i++) \{
    int first = Integer.valueOf(s.substring(i-1, i));
    int second = Integer.valueOf(s.substring(i-2, i));
    if (first >= 1) \{
    dp[i] += dp[i-1];
    \}
    if (second >= 10 && second &lt;= 26) \{
    dp[i] += dp[i-2];
    \}
    \}
    return dp[s.length()];
    \}
    
- Binary Tree Level Order Traversal
- Meeting Rooms II \{\{0,30\}, \{5,10\}, \{15,20\}\} => 2
    1. Create 2 arrays for starts & ends
    2. Sort starts & ends
    3. if start[i] &lt; ends[i] then increment start & counter
    4. If not increment both start & end
    5. return counter
- K Closeset Points to Origin
    1. Create a maxHeap order by Eucladian distance formula (furthest point away from the origin)
    2. Iterate theough the points placing each point into the heap and removing an element from the heap if the size grows over K
    3. Once the loop terminates, create another result array.
    4. Iterate until K > 0 and insert into result array removing from the heap.
- Max Area of Island
    1. Iterate through the entire matrix
    2. Every time you see a 1, run a dfs sinking each cell's neighbours while counting the area.
    3. Update the max if the current island area is greater than any other area you've encountered
    4. When you've iterated through the entire matrix, return the max
- Frog Jump
- MERGE K SORTED LISTS
    1. Iterate through every node in every list and add them to a minHeap
    2. Create a dummy node to hold the return list.
    3. Add every node from min heap to the list
    4. return dummy.next
- Grouped Anagrams
    1. Create a Map to hold the String as Key and List of Anagrams as value
    2. Iterate the given array of Strings
    3. Convert to char array
    4. Sort the char array
    5. Create a new String from the sorted char array
    6. Put the sorted string in the map if doesn't exist with new ArrayList() as value
    7. If exist, add the original sting to the list
    8. Add all values from the map to the list
    9. return list
- Unique Paths
    1. Dynamic Programming
    2. Create a dp[][] array
    3. Update first row & first column with value 1 as there are only step to reach that point
    4. Calculate the other cells in the matrix by adding the known values from the cells above dp[i][j] = dp[i-1][j] + dp[i][j-1]
    5. return dp[m-1][n-1]
- Walls and Gates
    1. DFS
    2. Iterate the Rooms
    3. If its a gate 0, then do dfs
    4. dfs
        1. check whether it goes out of bounds or rooms[i][j] &lt; count
        2. assign the count to the rooms for the distance to gate
        3. process dfs for adjacent rooms
- Happy Number
    1. Create a Set
    2. while n is not 1, process each digit, square and sum
    3. If the set contains the sum, return false (to avoid going into cycles)
    4. If not, add it to the set and continue the loops with the sum
    5. If loop exits, return true
- Word Search
    1. DFS
    2. Iterate the grid, if the char is same as tge first letter of the word and dfs of all adjacent chars match then return true
    3. else return false
    4. dfs (board, i, j, count, word)
        1. if the count match the length of the word, then we found the word return true
        2. If it gone out of bounds and the processing char is not the same as the word.charAt(count), return false
        3. Mark the current cell empty after copying the value to temp
        4. run dfs for all the remaining cells
        5. Copy the value from temp back to the cell
        6. return
- Valid Palindrome II
    1. Have 2 pointers - start & end
    2. Move both the pointers, if they are equal return true
    3. If they are not equal, check isPalindrome by excluding either of the characted from both start & end ing position isPalindrome(s, i+1, j) || isPalindrome(s, i, j-1)
    4. return true
- Longest Common Prefix
    1. Using the first String from the given array, check each character of the remaining Strings
    2. Exit condition: if the procesing string goes out of bounds or the character is not the same at the index of the processing string
    3. If they match, add the char to the result and increment index
    4. retun longestCommonPrefix
- Subsets
- PARTITION EQUAL SUBSET SUM
- MERGE TWO SORTED LISTS
    
    Approach #1:
    1. Create a PriorityQueue (sorted by natural ordering)
    2. Add both list to the PriorityQueue
    3. Create a dummy & head and add values to head from the PriorityQueue
    4. Return dummy.next
    Approach #2:
    1. Create a dummy node & head; head = dummy
    2. Compare nodes from each list and add thhe lowest to the head list
    3. Add the remaining either from list one or two to head
    4. return dummy.next
    
- COIN CHANGE
    1. DP
    2. Create a dp array for size given amount + 1
    3. Fill the dp array with some max value (amount + 1)
    4. for $0.0 we need 0 coins, hence dp[0] = 0
    5. for all the amount from 0 iterate for (int i = 0; i &lt;= amount; i++)
    a. for all the given coins for (int j = 0; j &lt; coiuns.length; j++)
    b. if the coin value is less than or equal to amount if (coin[j] &lt;= i)
    c. assign the min value to dp array dp[i] = Math.min(dp[i], 1 + dp[i - coins[j]])
    6. return -1 if the numbe of steps at amount index is max value or return the result
    return dp[amount] > amount ? -1 : dp[amount];
- BINARY TREE RIGHT SIDE VIEW
    1. Create a Queue Queue&lt;Integer> queue = new LinkedList&lt;>();
    2. Add to the queue
    3. While queue is not empty while (!queue.isEmpty())
    a. Get size of the queue int size = queue.size();
    b. Iterate for the size of the queue
    i. Remove the node from the queue and assign it to a temp node
    TreeNode current = queue.remove();
    ii. Add the value of the rightmost node to the result list (rightmost node is the last node in the queue)
    if (i == size - 1) result.add(current.val)
    iii. If there is a left node, add it to the queue if (current.left != null) queue.add(current.left)
    iv. If there is a right node, add it to the queue if (current.right != null) queue.add(current.right)
    4. return result list
- BINARY TREE PATHS
    1. DFS dfs(Node root, String path, List&lt;String> paths)
    2. Since root is not null, add root value to path; path += root.val;
    3. If you reach the leaf node, add the path to the result list if (root.left == null && root.right == null) \{ paths.add(path); \}
    4. If the root has a left child, call dfs with root's left dfs(root.left, path + "->", paths)
    5. If the root has a right child, call dfs with root's right dfs(root.right, path + "->", paths)
    6. return result list paths
- RANGE SUM OF BST
    1. Create a Queue
    2. Add root node to the queue. queue.add(root)
    3. While queue is not empty; while (!queue.isEmpty)
    4. Get the current node - remove from queue; TreeNode current = queue.remove()
    5. if the current node value is >= low && &lt;= high then add to sum; if (current.val >= low && current.val &lt;= high) \{ sum += current.val; \}
    6. If the current node has a left child and the current node value is > than low; then add its left child
    if (current.left != null && current.val > low) \{queue.add(current.left); \}
    7. If the current node has a right child and the current node value is &lt; than high; then add its right child
    if (current.right != null && current.val > high) \{queue.add(current.right); \}
    8. return sum.
- KTH SMALLEST ELEMENT IN A BST
    
    Approach #1:
    1. Create a List
    2. Do inOrder traversal and add all nodes to the list
    3. return list.get(k-1)
    Approach# 2: Iterative inOrder
    1. Create an int n = 0, temp root - cur = root;
    2. Create a Stack
    3. Go all the way down and push to stack
    while (true) \{
    while (cur != null) \{
    stack.push(cur);
    cur = cur.left;
    \}
    // pop from Stack
    cur = stack.pop();
    n++;
    if (n == k) \{
    return cur.val;
    \}
    // if not keep traversing
    cur = cur.right;
    \}
    
- ADD STRINGS O(m+n)
    1. Start from last character
    int i = num1.length() - 1
    int j = num2.length() - 1
    2. Initialize carry to 0; carry = 0
    3. while (i >= 0 || j >= 0)
    int sum = carry;
    if (i >= 0)
    sum += nums1.charAt(i--) - '0'
    if (j >= 0)
    sum += nums2.charAt(j--) - '0'
    sb.insert(0, sum % 10);
    carry = sum / 10;
    4. If carry is non 0, add it to result if (carry != 0) \{ sb.insert(0, carry); \}
    5. return result; sb.toString()
- VALIDATE BINARY SEARCH TREE
    1. Recursive call to validate(TreeNode root, Integer max, Integer min)
    2. If root is null, return true;
    3. Else if it violates the BST property then return false
    else if (max != null && root.val >= max || min != null && root.val &lt;= min) \{ return false; \}
    4. Else recursively call validate for both left & right nodes
    return validate(root.left, root.val, min) && validate(root.right, max, root.val)
- REORGANIZE STRING
    1. Create a Map to count the number of occurances of each character
    for (char c: s.toCharArray()) \{ map.put(c, map.getOrDefault(c, 0) + 1\}
    2. Create a PriorityQueue maxHeap sorted by occurances in map
    PQ&lt;Character> maxHeap=new PQ((a,b)-> map.get(b) - map.get(a))
    3. Add all keys to PQ
    maxHeap.addAll(map.keySet())
    4. while (maxHeap.size() > 1)
    5. remove top elements from PQ
    char current = maxHeap.remove();
    char next = maxHeap.remove();
    6. Add to result
    sb.append(current);
    sb.append(next);
    7. Reduce the count in the map
    map.put(current, map.get(current) - 1)
    map.put(next, map.get(next) - 1)
    8. If there are any remaining, add them back to maxHeap
    if (map.get(current) > 0)
    maxHeap.add(current)
    if (map.get(next) > 0)
    maxHeap.add(next)
    9. End while
    10. After processing if PQ is not empty and if there is ony one char left, add it to the result, else return empty string
    if (!maxHeap.isEmpty()) \{
    char last = maxHeap.remove();
    if (map.get(last) > 1) \{
    return "";
    \} else \{
    sb.append(last);
    \}
    \}
    11. return result. sb.toString();
- Verifying an Alien dictionary
    1. Create a mapping arry to hold the ordering
    for (int i = 0; i &lt; order.length(); i++) \{
    mapping[order.charAt(i) - 'a'] = i
    \}
    2. Compare with previous word to see if any characted is bigger
    for (int i = 1; i &lt; words.length; i++) \{
    if (bigger(words[i-1], words[i])) \{
    return false;
    \}
    \}
    return true;
    3. bigger (String s1, String s2) \{
    int m = s1.length(); int n = s2.length();
    for (int i = 0; i &lt; m && i &lt; n ; i++) \{
    if (s1.charAt(i) != s2.charAt(i)) \{
    return mapping[s1.charAt(i) - 'a'] > mapping[s2.charAt(i) - 'a']
    \}
    \}
    return m > n;
    \}
- Defanging an IP
- BINARY TREE LONGEST CONSECUTIVE SEQUENCE
- STRING COMPRESSION
